const projectsReducer = (
    state = { allProjects: [], chosenProject: {} },
    action
) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default projectsReducer;