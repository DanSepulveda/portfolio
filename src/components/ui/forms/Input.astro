---
import type { HTMLAttributes } from "astro/types";

import { baseInputStyle } from "../base-styles";
import ErrorMessage from "./ErrorMessage.astro";
import Label from "./Label.astro";

interface Props extends Omit<HTMLAttributes<"input">, "id"> {
  label: string;
  name: string;
}

const { label, name, type = "text", class: className, ...rest } = Astro.props;
---

<div class="flex flex-col gap-1.5">
  <Label for={name}>{label}</Label>
  <input
    id={name}
    name={name}
    type={type}
    class:list={["form-control", baseInputStyle, className]}
    aria-describedby={name + "-error-text"}
    {...rest}
  />
  <ErrorMessage name={name} />
</div>
